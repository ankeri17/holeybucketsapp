# Netlify build configuration for the Holey Buckets app.
# Connect this repo in the Netlify dashboard and it will deploy on every push.
# The official Next.js plugin handles routing/rendering automatically.

[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"
